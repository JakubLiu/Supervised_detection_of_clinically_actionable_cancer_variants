model1 --> fitted to the unfiltered negative control data

model2 --> fitted to the filtered egative control data

                  --> removed samples with no coverage
                  
                  --> removed samples that had an error rate of above 1%
